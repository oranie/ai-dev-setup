# Claude Code 開発ガイドライン (CLAUDE.md)

本リポジトリでは、Claude Code と Antigravity の併用環境で開発を行います。
エージェント共通の行動指針および7大禁止事項については、必ず [AGENTS.md](./AGENTS.md) を最優先で遵守してください。

---

## 1. Claude Code固有の運用ルール

### 1-1. リファレンス先行（Reference-First）の徹底
- 新機能の実装やライブラリ追加、設定ファイルの生成を行う前に、当てずっぽうなコード生成を禁止します。
- WebSearch や WebFetch ツール等を利用し、公式マニュアル、GitHub公開リポジトリ、技術記事を検索・確認してください。
- 調査した技術情報は、後続セッションやAntigravityと共有できるよう、`docs/references/` 配下にMarkdownで記録してください（テンプレート: `docs/references/template.md`）。
- 記録した技術リファレンスに基づいてコードや設定ファイルを生成してください。

### 1-2. テストコードの品質担保
- テストを追加・修正する際は、必ず戻り値や状態変化を検証する `assert` 文を含めてください。
- 単に関数を呼んで例外が出ないことだけを確認するテストは禁止します。
- 新規データソース追加時は、末端処理関数でのデータ実到達率（全対象に対する比率）をアサーションしてください。

### 1-3. Git操作の安全ルール
- `git add -A` や `git add .` を実行してはなりません。コミット対象のファイルを明示して `git add <file>` を実行してください。
- 認証トークン、秘密情報、ビルド成果物、巨大データファイルが混入していないか、コミット前に必ず差分を確認してください。

### 1-4. フロントエンド・UIの注意点
- CSSにおいて `!important` を追加してはなりません。セレクタの詳細度またはCSS変数で解決してください。
- `alert` や `confirm` 等のブラウザ標準ダイアログは使用せず、UIコンポーネントを使用してください。
- モバイル幅（375px）で横スクロールが発生しないことを確認してください。
