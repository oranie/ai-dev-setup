# Claude Code 開発ガイドライン (CLAUDE.md)

本リポジトリでは、Claude Code と Antigravity の併用環境で開発を行います。
エージェント共通の行動指針および禁止・遵守事項については、必ず [AGENTS.md](./AGENTS.md) を最優先で遵守してください。

**危険なコマンドは `.claude/settings.json` のフックが実行前に遮断します。**
判定は `guards/rules.py` にあり、Antigravity 側（`.agents/hooks.json`）と**同じ表**を読みます。
ルールを足すときは `guards/rules.py` だけを直してください。

---

## 1. Claude Code固有の運用ルール

### 1-1. リファレンス先行（Reference-First）の徹底
- 新機能の実装やライブラリ追加、設定ファイルの生成を行う前に、当てずっぽうなコード生成を禁止します。
- `reference-survey` スキル、または WebSearch / WebFetch を利用し、公式マニュアル、GitHub公開リポジトリ、技術記事を検索・確認してください。
- 調査した技術情報は、後続セッションやAntigravityと共有できるよう、`docs/references/` 配下にMarkdownで記録してください（テンプレート: `docs/references/template.md`）。
- 記録した技術リファレンスに基づいてコードや設定ファイルを生成してください。

### 1-2. 監査スキルの実行
実装が一段落したら、以下を適宜実行して品質を確かめてください（`.claude/skills/` 配下）。

| スキル | 何を見るか |
|---|---|
| `reference-survey` | 実装前の調査と `docs/references/` への記録 |
| `leak-and-wiring-audit` | データの配線抜け、値の捏造、未来データの参照 |
| `code-quality-audit` | アサーション欠落テスト、CSS の `!important`、ブラウザ標準ダイアログ |

### 1-3. テストコードの品質担保
- テストを追加・修正する際は、必ず戻り値や状態変化を検証する `assert` 文を含めてください。
- 単に関数を呼んで例外が出ないことだけを確認するテストは禁止します。
- 新規データソース追加時は、末端処理関数までデータが届いている割合をアサーションしてください。
- **テストを skip / 無効化して通してはいけません。** 落ちる理由のほうを直してください。

### 1-4. Git操作の安全ルール
- `git add -A` や `git add .` を実行してはなりません。コミット対象のファイルを明示して `git add <file>` を実行してください。
- 認証トークン、秘密情報、ビルド成果物、巨大データファイルが混入していないか、コミット前に必ず差分を確認してください。
- 履歴を上書きする場合は `--force` ではなく `--force-with-lease` を使ってください。

### 1-5. フロントエンド・UIの注意点（Webフロントエンドを含む場合）
- CSSにおいて `!important` を追加してはなりません。セレクタの詳細度またはCSS変数で解決してください。
- `alert` や `confirm` 等のブラウザ標準ダイアログは使用せず、UIコンポーネントを使用してください。
- モバイル幅（375px）で横スクロールが発生しないことを確認してください。

### 1-6. 推測即座実行（Eager Execution）の禁止と5大受入基準
- 指示を受け取った際、直ちにコード生成を開始せず、AGENTS.md 第3項の「5大受入基準（DoI）」を満たしているか照合してください。
- 「全部」「続けて」等の単文指示に対しては、全件走査や全選択肢実行を避け、番号選択式で確認してください。
- 3ファイル以上の変更、インフラ設定、出力される数値が変わる変更のときは、計画提示を先行させてください。
- 完了報告前に、AGENTS.md 第4項の自己点検（環境差、起動経路、生成物の整合性、原因を推測で報告しない）を行ってください。

