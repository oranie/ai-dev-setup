# Claude Code 固有の設定 (CLAUDE.md)

@AGENTS.md

行動規範と禁止・遵守事項は、上で読み込んでいる [AGENTS.md](./AGENTS.md) がすべてです。
このファイルには、Claude Code でしか意味を持たないことだけを書きます。
規約を足すときは AGENTS.md 側に書いてください。**同じことを 2 箇所に書かないこと**（AGENTS.md 2-A-5）。

---

## 1. 危険コマンドのガード

`.claude/settings.json` の `PreToolUse` フックが、Bash の危険なコマンドを実行前に遮断します。
判定は `guards/rules.py` にあり、Antigravity 側（`.agents/hooks.json`）と**同じ表**を読みます。
ルールを足すときは `guards/rules.py` だけを直してください。

遮断されたら、`--no-verify` や別の書き方で回避せず、**止められた理由のほうを解消**してください。

## 2. 監査スキル（`.claude/skills/` 配下）

実装が一段落したら、以下を適宜実行して品質を確かめてください。

| スキル | 何を見るか |
|---|---|
| `reference-survey` | 実装前の調査と `docs/references/` への記録 |
| `leak-and-wiring-audit` | データの配線抜け、値の捏造、未来データの参照 |
| `code-quality-audit` | アサーション欠落テスト、CSS の `!important`、ブラウザ標準ダイアログ |

- `reference-survey` の代わりに WebSearch / WebFetch を使っても構いませんが、調べた結果は
  どちらの場合も `docs/references/` に残してください（テンプレート: `docs/references/template.md`）。
  後続のセッションと Antigravity が、同じ調査を繰り返さずに済みます。
- モデルを移行したら `/claude-api prompt-audit` で `AGENTS.md`・`CLAUDE.md`・`.claude/agents/` を監査してください。
  古いモデル向けの強調、検算の儀式、矛盾した指示、廃止された設定値を拾います
  （`docs/references/claude-platform-cost.md`）。
- スキルの本体は `.agents/skills/` と**同じ内容**です。片方だけ直すと
  `tests/test_hook_wiring.py` が落ちます。直すときは両方です。

## 3. 委譲（`.claude/agents/` のサブエージェント）

モデルへの入力は毎ターン全履歴が送り直されます。**テストの全件出力を 1 回会話に載せると、
以降の全ターンで再送されます**（AGENTS.md 2-A-9）。サブエージェントは会話履歴を引き継がない
別コンテキストで動くので、生ログは向こうに閉じ、本体には要約だけが返ります。

### いつ投げるか

1. **出力が数百行を超えるか。** 超えないなら自分でやる。委譲は無料ではなく、向こうは規約を
   読み直すところから始まります。grep 数行、テスト 1 件の再実行は自分でやったほうが安い
2. **本体のモデルが高いか。** 本体が Fable 5.1 や Opus 5.5 のときは、計画・判断・レビューだけを
   手元に残し、決まった実行と範囲の決まった実装を出します。本体が Sonnet で動いているときは、
   この理由での委譲は効きません（往復のぶん高くつきます）
3. **結果を見て判断や修正が要るか。** 要るなら**実行だけ**投げて判断は手元に残す。
   要らない（調べて記録するだけ）なら丸ごと投げる。
   やることをプランに書き切れるなら、実装ごと `implementer` に出す

### 誰に投げるか

| agent | モデル | 何を任せるか | 返るもの |
|---|---|---|---|
| `test-runner` | `claude-sonnet-5` | テスト・検証の実行 | 落ちたものだけ、30 行以内 |
| `repo-scout` | `claude-sonnet-5` | コードの在処探し | パスと行番号、20 行以内 |
| `reference-surveyor` | `claude-sonnet-5` | 公式ドキュメント調査と `docs/references/` への記録 | 書いたパスと結論 5 行 |
| `implementer` | `claude-opus-5-5` | プラン通りのコード生成（テスト追加を含む） | 変更一覧・数字・**止めたところ**、30 行以内 |
| `implementer-lite` | `claude-sonnet-5` | 決まった実行、文書の修正、定数・閾値の書き換え | 同上、20 行以内 |

線は **「コードを生成するかどうか」** です。コードを書かない作業に `implementer` の単価を払わない。

### 委譲しないもの

`git push`、PR の作成、マージ、本番環境に影響する操作。**手元に残します。**

### 委譲プロンプトの書き方

**会話履歴は渡りません。**「さっき話したあれ」は通じません。届くのは `CLAUDE.md` と
`AGENTS.md`、それと委譲プロンプトだけです。次の 4 つを書いてください。

1. **どこで / 何を**（絶対パスとコマンド、または**プランのファイルのパス**）
2. **前提**（この環境の制約、既に分かっていること）
3. **返す形**（何行以内、何を含め、何を含めないか）
4. **やらないこと**（直さない、提案しない、プランに無い判断が出たら止まる）

実装を委譲するときは、**触らせない範囲**も書きます。並行作業とぶつかると、こちらでは気づけません。

### 注意

- `.claude/agents/` に足しただけでは使えません。**セッションを開き直してください**
- `model:` は `sonnet` のようなエイリアスで書かないこと。プロバイダによって世代が変わります。
  フル ID で固定し、`tests/test_subagents.py` でも固定しています
- **hook はサブエージェントの中でも走ります。** 委譲しても遮断は迂回されません
- **委譲先のキャッシュは本体と共有されません。** 共有されるのは、同じモデルでプレフィックスが
  バイト単位で一致するときだけです。別モデルの agent は毎回キャッシュ無しで始まります。
  「委譲は無料ではない」の中身の大半はこれです
- **キャッシュの有効期限（TTL）より長く走る委譲は、本体のキャッシュを失効させます。** 戻った次のターンで、
  本体は全履歴を定価で読み直します。長くなる実行は分割して投げてください
- **セッションの途中で `/model` を切り替えない。** キャッシュが切れます。effort の変更は
  Opus 5 系と Fable 5.1 ならキャッシュを切りません
- agent の `effort:` は、**同じ作業で段階を振って測ってから**入れてください（AGENTS.md 2-A-8）。
  強いモデルを低い effort で回すほうが、弱いモデルを高い effort で回すより安いことがあります
- 委譲できているかは `/usage` の内訳で見えます。本体の入力トークンが減っていなければ、
  委譲できていないか、委譲しすぎて往復が増えているかのどちらかです

## 4. PR を着地させる（`/land`）

PR を出したあと、CI が緑になるのを待ってマージし、関連 Issue とドキュメントを片付けて
セッションを閉じるまでを `/land` が持っています（`.claude/skills/land/`）。
**PR を出して終わりにしない**という規約（AGENTS.md 2-A-10）の実行役です。

- `/land`（引数なし）で今のブランチの PR、`/land 42` で PR 番号を指定
- **PR は作りません。** 出すところまでは自分でやってから呼ぶこと
- `gh` が要ります。CI を待つのに `sleep` でポーリングしないこと
- 赤やコンフリクトで判断が要るときは、止めてユーザーを呼ぶ場面です
