# Antigravity 開発ガイドライン (GEMINI.md)

本リポジトリでは、Antigravity（Gemini）とClaude Codeの併用環境で開発を行います。
エージェント共通の行動指針および禁止・遵守事項については、必ず [AGENTS.md](./AGENTS.md) を最優先で遵守してください。

**危険なコマンドは `.agents/hooks.json` のフックが実行前に遮断します。**
判定は `guards/rules.py` にあり、Claude Code 側（`.claude/settings.json`）と**同じ表**を読みます。
ルールを足すときは `guards/rules.py` だけを直してください。

---

## 1. Antigravity固有の運用ルール

### 1-1. リファレンス先行（Reference-First）の徹底
- 新しい機能の実装、外部APIの連携、インフラ設定の変更を命じられた場合、いきなりコード生成を行わないこと。
- まず `reference-survey` スキルを活用し、公式ドキュメントやリポジトリの仕様を調査して `docs/references/` 配下にMarkdownで記録すること。
- 調査結果を元に実装計画（implementation_plan）を立て、確認した正確なシグネチャ・設定値のみを用いてコードを記述すること。

### 1-2. スキルと監査の実行
- 実装完了後は、以下の監査スキルを適宜実行して品質を担保すること。
  - `leak-and-wiring-audit`: データの配線抜け、値の捏造、未来データの参照
  - `code-quality-audit`: アサーション欠落テスト、CSS `!important`、ブラウザ標準ダイアログの検出
- スキルの本体は `.agents/skills/` にあり、`.claude/skills/` と**同じ内容**です（食い違わないことをテストで縛っています）。

### 1-3. トークン消費の最適化
- ターミナルでコマンドを実行する際、数千行に及ぶ大量の生ログをそのまま標準出力に出さないこと。`grep`, `head`, `tail`, `jq` 等を用いて必要な箇所に絞り込んで確認すること。
- スクラッチスクリプトを作成して検証を行う場合は、ワークスペースを汚さず `.gemini/antigravity/brain/<conversation-id>/scratch/` または `scripts/scratch/` を使用すること。

**大きい出力の逃がし方は Claude Code と異なります。**
AGENTS.md 2-A-9 は「出力が数百行を超える実行は本体の会話の外へ出す」と定めていますが、
Claude Code が使う `.claude/agents/` のサブエージェントに当たる仕組みは Antigravity にはありません。
このツールで作業するときは、**文脈の圧縮と保持はハーネスに任せ**、こちらでは次の 2 つだけを守ってください。

- コマンドの出力を**渡す前に絞る**（`grep` / `head` / `tail` / `jq`）。丸ごと会話に載せない
- 検証用のスクラッチをリポジトリ直下に置かない

`.claude/agents/` の定義は Antigravity からは読まれません。
**道具の名前で指示を書かないこと**（「`test-runner` に投げて」は Antigravity では空振りします）。

### 1-4. Git操作の安全ルール
- ステージング時は `git add -A` や `git add .` を使用せず、変更したファイルパスを明示して実行すること。
- `.agents/hooks.json` にて危険コマンドの抑止フックが設定されているため、フックの指示に従うこと。
- 履歴を上書きする場合は `--force` ではなく `--force-with-lease` を使うこと（フックは前者だけを止める）。

### 1-5. 推測即座実行（Eager Execution）の禁止と5大受入基準
- ユーザー指示を受け取った際、直ちにコード生成に入らず、AGENTS.md 第3項の「5大受入基準（DoI）」を照合すること。
- 3ファイル以上の変更、インフラ変更、出力される数値が変わる変更に該当する場合は、必ず実装計画書（`implementation_plan.md`）を作成してユーザー承認を得ること。
- 前提不足を検知した際は、番号選択式の逆質問フォーマットを用いて確認すること。
- 完了報告の前に、AGENTS.md 第4項の提出前点検（環境差、起動経路、生成物の整合性、原因を推測で報告しない）を必ず実行すること。

