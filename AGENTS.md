# 開発行動規範 (AGENTS.md)

このファイルは、本プロジェクトで作業するすべてのAIエージェント（Antigravity, Claude Code等）に対する共通指示です。
過去の開発実績（246コミット、83件の不具合・ハルシネーション統計）に基づき、同じ失敗を繰り返さないための必須手順と禁止事項を定めます。

---

## 1. 最優先原則: リファレンス先行（Reference-First）

コード、IaC、設定ファイルの生成に着手する前に、必ず以下の手順で技術情報の調査と蓄積を行ってください。

1. **先行調査の実施**:
   - 利用するライブラリ、API、インフラの公式マニュアル、GitHubの公開リポジトリ、または信頼できる技術ブログを検索・閲覧する。
   - 推測や記憶に頼ってAPIのシグネチャ、設定値、クラウドIAM権限を記述してはならない。
2. **技術メモの蓄積**:
   - 調査した仕様、設定構文、既知の落とし穴を `docs/references/` 配下のMarkdownファイルに記録する（テンプレート: `docs/references/template.md`）。
   - 後続のプロンプトや別のエージェントセッションが、無駄な再調査を行わずに参照できるようにする。
3. **リファレンスに基づく計画立案**:
   - `docs/references/` に記録した正確な情報を足場にして、実装・修正の計画を立てる。

---

## 2. 7大禁止・遵守事項

### 2-1. 未来データの参照防止（データリーク防止）
- 特徴量抽出関数には、対象イベントの発生日時（`match_datetime`等）以前のデータのみを渡すこと。確定スコア、最終順位、試合後スタッツへのアクセスを禁止する。
- 対戦カード照合時は、大会名だけでなく開催日時の完全一致を照合条件とし、未開催カードに対して過去の結果を照合してはならない。
- 修正・追加後はリーク監査を実行し、目的変数との異常相関がないことを確認すること。

### 2-2. 配線抜け・幽霊コードの防止
- 新しいデータソースや実績テーブルを追加した際は、データファイルを配置して満足せず、末端の処理関数での実到達率（全対象に対する有効データの割合）をテストコードでアサーションすること。
- データパース処理にはカラム数とデータ型の厳格なバリデーションを入れ、列ズレやヘッダ欠落を黙殺せずログを出力すること。
- 機能廃止やリファクタリングで不要になったコードは速やかに削除し、未参照コードを放置しないこと。

### 2-3. ダミー採点・捏造数式の厳禁
- 登録番号の剰余（`idx % 6`）や固定値によるスコアリング関数の実装を厳禁とする。
- データが存在しない場合は数値を捏造せず、明示的な欠損値（NaN）または事前定義された補正値を適用すること。
- 過去データのない対象（新規参入者、ルーキー等）に対し、機械的に最低ランクを割り当ててはならない。全体分布に基づいた初期値を適用すること。

### 2-4. 形骸化テストの防止
- テストコードには、必ず検証対象の具体的な戻り値や状態変化に対するアサーションを含めること。
- 単に関数を実行して例外が出ないことだけを確認するテスト（アサーションなしテスト）を禁止する。
- テスト自体が暗黙に未来データを参照してパスするような偽装テストを記述してはならない。

### 2-5. インフラ・起動性能の担保
- Webアプリケーションの起動処理（ヘルスチェック完了前）に、全件DBスキャンや重い集計処理を含めてはならない。起動処理は5秒以内に完了させ、整合性確認はデプロイ前のCIで実施すること。
- 分散コンテナ環境から、ネットワークストレージ上の単一ファイルDB（SQLite等）へ大量の一括書き込みを行ってはならない。更新処理はバッチ専用ワーカーに分離すること。
- 新規コンテナ起動時に重い相関集計を行わず、事前に永続化テーブルにキャッシュして数ミリ秒で読み出すこと。
- リポジトリにデータやキャッシュを追加する際は、必ず `.dockerignore` を同時に更新し、ビルドコンテキストが肥大化しないことを確認すること。

### 2-6. UI・フロントエンドのアンチパターン防止
- CSSにおける `!important` の新規追加を厳禁とする。既存スタイルのセレクタ詳細度またはCSS変数を修正すること。
- ブラウザ標準のダイアログ（`alert`, `confirm`）の使用を禁止し、共通UIコンポーネントを使用すること。
- モバイル解像度（幅375px）で意図しない横スクロールが発生していないことを確認すること。

### 2-7. Git操作とセキュリティの安全基準
- `git add -A` や `git add .` の実行を禁止する。変更した対象ファイルを明示して `git add path/to/file` でステージングすること。
- トークン、秘密鍵、一時ファイル、ビルドキャッシュを誤コミットしないよう、事前に `git status` で差分を確認すること。
- クラウド権限（IAM）の当てずっぽうな推測を禁止する。権限不足エラー（403等）が発生した際は、公式リファレンスまたは権限照会コマンドで正確に必要なアクションを特定すること。

---

## 3. 推測による即座実行（Eager Execution）の禁止とトリアージ規約

AIエージェントは「指示を受けたら直ちにコードを生成し、コマンドを実行する」行動バイアスを持ちます。情報が欠落している際に推測で補完して走る行為は、未来データ参照（パターン③）、数値捏造、偽テスト作成を招き、所要時間を4.7倍（18.5分）、応答ターンを2.1倍（68.4ターン）に膨張させます。

エージェントは新しい指示を受領した際、以下の**5大受入基準（Definition of Input: DoI）**に照らして入力を検証し、不足がある場合は作業を保留して確認質問を行ってください。

### 5大受入基準（DoI）
1. **スコープの特定**:
   - 対象ファイル、ディレクトリ、関数名が名指しされているか。「全部」「あれ」「いい感じに」等の曖昧指定は保留する。
2. **ドメイン制約・フォールバック**:
   - 特徴量計算や集計処理において、未来データ除外、欠損時の扱い（NaN / 中立値）、例外按分ルールが指定されているか。
3. **新規技術のリファレンス**:
   - 外部ライブラリ、クラウド設定、GitHub Actions変更において、`docs/references/` に公式調査メモが存在するか。
4. **完了条件（DoD）の明示**:
   - 「例外が出ないこと」だけでなく、どの戻り値・画面項目をアサートするか、CI確認まで含むかが明示されているか。
5. **選択の丸投げ検知**:
   - 選択肢提示直後の「続けて」「まかせる」に対し、全件実行せず「どの番号（案）を進めるか」を確認する。

### 計画モードへの強制引き上げ条件
以下のいずれかに合致する場合、AIはコード生成に着手せず、必ず実装計画書（`implementation_plan.md`）を作成してユーザー承認を得ること。
- 変更対象が 3 ファイル以上に跨がる場合
- 外部ライブラリ、クラウドインフラ（GCP等）、CI/CDワークフローの設定変更
- 予測モデルの特徴量抽出・評価ロジックの変更

### 逆質問フォーマット（番号選択式）
基準未達の指示を検知した場合、自由記述を求めず、ユーザーが番号で回答できる形式で逆質問してください。

```text
【プロンプト確認】前提条件の指定をお願いします。
1. 対象スコープ: （1: backend/scoring.py のみ / 2: 全集計モジュール）
2. データ欠損時の扱い: （1: 欠損値NaNとする / 2: 全体平均で補正）
3. 完了条件: （1: 単体テスト通過まで / 2: CIログ確認まで）
※番号でお答えいただくだけで進行できます。
```

---

## 4. 提出前自己点検チェックリスト（パターン⑤対策: 実装側の確認不足防止）

指示側に不備がない場合でも発生する手戻り（実行環境差、本番競合、生成物の初歩的破綻）を防ぐため、完了報告の前に以下の3点を自己点検してください。

1. **実行環境差の検査**:
   - 新規 import したライブラリがロックファイル（`requirements.lock.txt` 等）に定義されているか。ローカル環境固有のパッケージに依存していないか。
2. **起動シーケンスの整合性検査**:
   - 変更したコードがWebアプリの起動経路（ヘルスチェック完了前）を通る場合、重い処理やDBスキャンを含めていないか（5秒以内起動）。
   - デプロイ時の新旧コンテナ重複起動時に、単一ファイルDB（SQLite等）への衝突を起こさないか。
3. **生成物のドメイン整合性検査**:
   - 画像生成やUI変更の場合、出力項目に初歩的な破綻（選手欄にチーム名が入る、ポジションと無関係なパラメータの重複等）がないか、出力直後に自己検証する。

