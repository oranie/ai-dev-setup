# 開発行動規範 (AGENTS.md)

このファイルは、本プロジェクトで作業するすべてのAIエージェント（Antigravity, Claude Code等）に対する共通指示です。
実際の開発で発生した不具合の分析に基づき、同じ失敗を繰り返さないための必須手順と禁止事項を定めます。

**第2章は「すべてのプロジェクトに適用するもの」と「該当する場合に適用するもの」に分かれています。**
自分のプロジェクトに関係しない節（2-A-6 のUI、2-B-1 のデータリーク等）は、そのまま削除して構いません。
規約は短いほど守られます。関係ないものを残すと、エージェントは規約全体を読み飛ばします。

---

## 1. 最優先原則: リファレンス先行（Reference-First）

コード、IaC、設定ファイルの生成に着手する前に、必ず以下の手順で技術情報の調査と蓄積を行ってください。

1. **先行調査の実施**:
   - 利用するライブラリ、API、インフラの公式マニュアル、GitHubの公開リポジトリ、または信頼できる技術ブログを検索・閲覧する。
   - 推測や記憶に頼ってAPIのシグネチャ、設定値、クラウドIAM権限を記述してはならない。
2. **技術メモの蓄積**:
   - 調査した仕様、設定構文、既知の落とし穴を `docs/references/` 配下のMarkdownファイルに記録する（テンプレート: `docs/references/template.md`）。
   - 後続のプロンプトや別のエージェントセッションが、無駄な再調査を行わずに参照できるようにする。
3. **リファレンスに基づく計画立案**:
   - `docs/references/` に記録した正確な情報を足場にして、実装・修正の計画を立てる。

---

## 2. 禁止・遵守事項

### 2-A. すべてのプロジェクトに適用する

#### 2-A-1. 配線抜け・幽霊コードの防止
- 新しいデータソースや依存モジュールを追加した際は、ファイルを配置して満足せず、**末端の処理関数までデータが届いている割合**をテストコードでアサーションすること。
- 入力のパース処理には項目数と型の検証を入れ、欠落や桁ズレを黙殺せずログを出力すること。
- 機能廃止やリファクタリングで不要になったコードは速やかに削除し、未参照コードを放置しないこと。

#### 2-A-2. 値の捏造の厳禁
- 入力の識別子や添字から機械的に値を作る実装（`idx % 6` のような剰余、連番からの逆算、固定値）を厳禁とする。
- データが存在しない場合は数値を捏造せず、**明示的な欠損値、または中立値**を用いること。どちらを使うかは呼び出し側が判断できるようにする。
- 実績が無い対象に機械的な最低評価を割り当ててはならない。全体分布に基づいた初期値を用いること。

#### 2-A-3. 形骸化テストの防止
- テストコードには、必ず検証対象の具体的な戻り値や状態変化に対するアサーションを含めること。
- 単に関数を実行して例外が出ないことだけを確認するテスト（アサーションなしテスト）を禁止する。
- **テストを skip / 無効化して通してはならない。** 落ちる理由のほうを直すこと。
- 検査を目的としたスクリプトは、**失敗時に非ゼロの終了コードを返すこと**を必ず確認する。常に 0 を返す検査は、検査したつもりで何も検査していない。

#### 2-A-4. 起動処理とデプロイ時の安全性
- サーバプロセスの起動処理（ヘルスチェック完了前）に、全件スキャンや重い集計を含めてはならない。起動は数秒で完了させ、重い検証はCIで先に行うこと。
- **起動処理は、やることが無ければ書き込みを 1 バイトも行わない作りにすること。**
  ローリングデプロイでは、新しいインスタンスが起動する時点で**古いインスタンスがまだ動いています**。「同時に動くのは 1 つ」という前提で共有ストレージ上のファイルを扱っていると、**この瞬間だけ前提が破れて**起動に失敗します。「インスタンス数を 1 に固定してあるから安全」は、デプロイ中には成立しません。
- 共有ストレージ（ネットワークマウント等）上の単一ファイルDBへ、複数プロセスから書き込みを行ってはならない。更新は専用のワーカーに分離すること。
- リポジトリにデータやキャッシュを追加する際は、`.dockerignore` を同時に更新し、ビルドコンテキストが肥大化しないことを確認すること。

#### 2-A-5. 定義を 2 箇所に置かない
- 同じ規則・同じ表・同じ設定を複数のファイルへコピーしないこと。**片方だけ直して食い違う**。
- 事情があって複数箇所に必要な場合（エージェントごとの設定ディレクトリ等）は、**一致していることをテストで縛る**こと。縛りが無いコピーは必ず食い違う。
- 設定ファイルが指すスクリプトのパスは、**実在することをテストで確認する**こと。フックは見つからなくても黙って素通りするため、壊れていることに気づけない。

#### 2-A-6. Git操作とセキュリティの安全基準
- `git add -A` や `git add .` の実行を禁止する。変更した対象ファイルを明示して `git add path/to/file` でステージングすること。
- トークン、秘密鍵、一時ファイル、ビルドキャッシュを誤コミットしないよう、事前に `git status` で差分を確認すること。
- 履歴を上書きする場合は `--force` ではなく `--force-with-lease` を使うこと。
- クラウド権限（IAM等）の当てずっぽうな推測を禁止する。権限不足エラー（403等）が発生した際は、公式リファレンスまたは権限照会コマンドで正確に必要な権限を特定すること。**記憶で断定したくなったら、それが調べるべき合図。**

#### 2-A-7. 変更はCIが検証できる形にする
- **Pull Request の時点でテストが走っているかを確認すること。** 既定ブランチへの push でしかCIが動かない構成では、**テストが走るのは常にマージした後**になり、壊れた変更がそのまま取り込まれる。
- CIが入れる依存と手元の依存が一致していることを確認すること。ローカルにだけ入っているパッケージに依存したコードは、**手元では通りマージ後に落ちる**。
- 新しく import した外部ライブラリが、ロックファイル（`requirements.lock.txt`、`package-lock.json` 等）に定義されているかを提出前に確認すること。

#### 2-A-8. 測ってから入れる・外す
- 性能改善、アルゴリズム変更、パラメータ調整は、**前後を同じ条件で測ってから**採否を決めること。良くならなければ入れない。
- 測った数値は、後から検証できる形で記録に残すこと（コミットメッセージ、`docs/` 配下など）。
- 「やらないと決めたこと」も理由付きで記録すること。記録が無いと、同じ検討を何度も繰り返す。

---

### 2-B. 該当する場合に適用する

> 以下はプロジェクトの性質に依存します。**関係しない節は削除してください。**

#### 2-B-1. 時系列データ・予測処理を扱う場合: 未来データの参照防止
- 特徴量や集計の入力には、**対象イベントの発生時刻より前のデータのみ**を渡すこと。確定結果、最終状態、事後に付与された属性へのアクセスを禁止する。
- レコードの突き合わせは、名称だけでなく**日時の一致**を条件に含めること。未確定のレコードに過去の結果を結びつけてはならない。
- 修正・追加後はリーク監査を実行し、目的変数との異常な相関がないことを確認すること。
- **評価指標が急に良くなったら、まずリークを疑うこと。** 実力が上がったのではなく、答えを見ている可能性のほうが高い。

#### 2-B-2. Webフロントエンドを含む場合: UIのアンチパターン防止
- CSSにおける `!important` の新規追加を厳禁とする。セレクタの詳細度またはCSS変数で解決すること。
- ブラウザ標準のダイアログ（`alert`, `confirm`）の使用を禁止し、共通UIコンポーネントを使用すること。
- モバイル解像度（幅375px）で意図しない横スクロールが発生していないことを確認すること。

## 3. 推測による即座実行（Eager Execution）の禁止とトリアージ規約

AIエージェントは「指示を受けたら直ちにコードを生成し、コマンドを実行する」行動バイアスを持ちます。情報が欠落している際に推測で補完して走る行為は、値の捏造、偽テストの作成、前提を誤った実装を招きます。実測では、この型に分類された指示は**所要時間が 4.7 倍（中央値 18.5 分 対 3.9 分）、応答ターンが 2.1 倍**に膨張しました（2026-09-16 時点の実測）。

エージェントは新しい指示を受領した際、以下の**5大受入基準（Definition of Input: DoI）**に照らして入力を検証し、不足がある場合は作業を保留して確認質問を行ってください。

### 5大受入基準（DoI）
1. **スコープの特定**:
   - 対象ファイル、ディレクトリ、関数名が名指しされているか。「全部」「あれ」「いい感じに」等の曖昧指定は保留する。
2. **ドメイン制約・フォールバック**:
   - 集計・変換処理において、**欠損時の扱い**（欠損のままか中立値か）、端数や重複の扱い、
     対象を絞る条件が指定されているか。時系列を扱う場合は、未来データの除外条件が指定されているか。
3. **新規技術のリファレンス**:
   - 外部ライブラリ、クラウド設定、CI/CD の変更において、`docs/references/` に調査メモが存在するか。
4. **完了条件（DoD）の明示**:
   - 「例外が出ないこと」だけでなく、どの戻り値・画面項目をアサートするか、CI確認まで含むかが明示されているか。
5. **選択の丸投げ検知**:
   - 選択肢提示直後の「続けて」「まかせる」に対し、全件実行せず「どの番号（案）を進めるか」を確認する。

### 計画モードへの強制引き上げ条件
以下のいずれかに合致する場合、AIはコード生成に着手せず、必ず実装計画書（`implementation_plan.md`）を作成してユーザー承認を得ること。
- 変更対象が 3 ファイル以上に跨がる場合
- 外部ライブラリ、クラウドインフラ、CI/CDワークフローの設定変更
- 出力される数値や判定結果が変わる変更（集計ロジック、スコアリング、評価基準）

### 逆質問フォーマット（番号選択式）
基準未達の指示を検知した場合、自由記述を求めず、ユーザーが番号で回答できる形式で逆質問してください。

```text
【プロンプト確認】前提条件の指定をお願いします。
1. 対象スコープ: （1: src/scoring.py のみ / 2: 集計モジュール全体）
2. データ欠損時の扱い: （1: 欠損のまま扱う / 2: 全体分布から補正する）
3. 完了条件: （1: 単体テスト通過まで / 2: CI通過の確認まで）
※番号でお答えいただくだけで進行できます。
```

---

## 4. 提出前の自己点検（指示に不備が無くても起きる手戻りを防ぐ）

指示側に不備がない場合でも発生する手戻り（実行環境差、本番競合、生成物の初歩的破綻）を防ぐため、完了報告の前に以下の3点を自己点検してください。

1. **実行環境差の検査**:
   - 新規 import したライブラリがロックファイル（`requirements.lock.txt`、`package-lock.json` 等）に
     定義されているか。手元にだけ入っているパッケージに依存していないか。
   - **手元で通ったことは、CIで通ることを意味しません。** CIが入れる依存だけで動くかを確かめること。
2. **起動シーケンスの整合性検査**:
   - 変更したコードが起動経路（ヘルスチェック完了前）を通る場合、重い処理を含めていないか。
   - **デプロイ中に新旧インスタンスが重なる瞬間**、共有ストレージ上のファイルへ書き込みが衝突しないか。
   - 可能であれば、**本番と同じ形**（同じコンテナ・同じ既存データ）で起動を確かめてから提出すること。
3. **生成物の整合性検査**:
   - 生成した出力（画面、レポート、画像等）に初歩的な破綻がないか、**出力直後に自分で読み返す**こと。
     項目の取り違え、単位の不一致、同じ値の重複は、実行が成功していても起きる。

4. **原因は推測で報告しない**:
   - 失敗の原因を、ログや再現で確かめる前に断定しないこと。推測のまま直すと、
     **無関係な箇所を壊したうえで再発**する。
   - 「一過性の失敗」で片付ける前に、**落ち方そのもの**を疑うこと。接続やリソースの後始末が
     漏れていると、次の失敗が原因と無関係な症状に化ける。

